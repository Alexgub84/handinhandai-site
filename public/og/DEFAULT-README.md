# Default OG image

`default.jpg` is the fallback social-sharing card used by `SEOHead.astro`
when a page doesn't pass its own `ogImage` prop.

**TODO (Alex):** the current file is a placeholder copy of
`public/og/fitness/og.jpg`. Replace with a proper 1200×630 Hand in Hand AI
brand card before broad launch.

Per-route OG images already in place:
- `/fitness-studio` → `public/og/fitness-studio/og.jpg`
- `/fitness-chain` → `public/og/fitness-chain/og.jpg`
- legacy `/fitness` → `public/og/fitness/og.jpg`
