---
import HomeDocument from "@components/handinhand/HomeDocument.astro";

/* Only `/en` is generated — it serves the same Hebrew homepage as `/`
   (canonical → `/`). Hebrew is the default and lives at `/`, so no `/he`. */
export function getStaticPaths() {
  return [{ params: { lang: "en" } }];
}
---

<HomeDocument />
